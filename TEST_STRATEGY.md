# Test Strategy

This document defines how `elroy-rs` should prove parity with `../elroy`.

The rewrite should rely on tests as the primary proof of correctness. Architecture and code review matter, but they are not substitutes for executable parity checks.

## Principles

- test user-visible behavior, not just implementation details
- document what each test layer proves
- prefer small deterministic fixtures over broad implicit setup
- use Python behavior and fixtures as the baseline where practical
- record known coverage gaps in `PARITY_MATRIX.md`

## Test Layers

### Unit Tests

Use for:

- config parsing and precedence
- path/layout derivation
- prompt/builder logic
- pure decision logic
- parser behavior

These should be fast, deterministic, and cheap to run continuously.

### Store / Persistence Tests

Use for:

- file-backed storage behavior
- migration logic
- schema and query behavior
- rebuild/bootstrap of derived DB state from file inputs

These should use realistic temporary file trees and database fixtures.

### Orchestrator Tests

Use for:

- turn processing workflows
- tool-call execution ordering
- context loading and persistence
- recall and due-item injection rules

These should validate workflow semantics without requiring the full TUI.

### Golden / Snapshot Tests

Use for:

- tool schema output
- streaming event ordering
- formatted prompt/context payloads
- stable CLI-visible reports where format matters

These should only be used where stable textual structure is part of the contract.

### End-To-End Tests

Use for:

- CLI entry behavior
- TUI interaction flows
- bootstrap from a compatible Elroy home directory
- Codex workflow integration once implemented

These should cover the default workflows users actually rely on.

## Required Coverage By Area

| Area | Minimum expectation |
| --- | --- |
| Config | unit tests for defaults, file loading, env precedence, compatibility keys |
| File-backed memory and agenda data | persistence tests using realistic file trees |
| DB bootstrap/rebuild | persistence or integration tests starting from compatible files |
| Tool system | golden tests for schemas plus orchestrator tests for execution behavior |
| Conversation loop | orchestrator tests for event ordering and persistence effects |
| TUI | end-to-end or UI integration tests for keybindings and focus behavior |
| Codex workflow | integration tests for persistence and lifecycle behavior |

## Documentation Rule

Each subsystem should make it easy to answer:

- what Python behavior is this test proving?
- what compatibility guarantee does this suite cover?
- what is still intentionally untested?

## Current Baseline

Current implemented coverage:

- `elroy-config`: defaults, YAML parsing, unknown-key tolerance, env override precedence, base-tool gating/exclusion config (`include_base_tools`, `exclude_tools`), greeting/bootstrap context controls, and context-refresh token controls
- `elroy-core`: session and turn boundaries plus provider-neutral conversation/tool-loop orchestration with normalized transcript accumulation, lazy streamed tool-loop support, and live-provider runtime wiring
- `elroy-db`: bootstrap planning, recursive markdown discovery including separate archived-memory discovery, migration execution, frontmatter parsing, persisted bootstrap inventory, derived memory/agenda table rebuilds including inactive archived-memory history rows, and context-message persistence
- `elroy-memory`: file-backed memory filename handling plus create/update/archive operations
- `elroy-agenda`: file-backed agenda create/update/complete/delete operations plus checklist add/edit/complete behavior, including returned update timestamps
- `elroy-feature-requests`: markdown-backed feature-request create/load/list/update flows, duplicate matching, and active self-reflection filtering
- `elroy-self-reflection`: correction-triggered proposal generation, cadence gating, dedupe/reopen behavior, and disabled-zero semantics
- `elroy-db`: memory-operation-tracker migration and round-trip persistence
- `elroy-db`: agenda-only vs due-item query separation plus derived checklist counts
- `elroy-db`: context-message-set get-or-create stability for one active set per user
- `elroy-app` + `elroy-db` + `elroy-agenda`: active/inactive due-item query behavior, Python-style timed-due prompt surfacing including multiple simultaneous due items plus app-level prompt/runtime surfacing, contextual-trigger surfacing including persisted current-context pinning and later-turn dedupe for matched reminders, plus direct helper/runtime cases where contextual due-item surfacing now broadens beyond zero-overlap wording misses through the same best-effort model-backed bounded candidate expansion used in recall quality, can now prefer a semantically relevant reminder over a weaker lexical-overlap candidate instead of only broadening from the zero-overlap case, and does not duplicate a reminder that is already present in same-turn fast-recall metadata, hybrid timed-reminder surfacing, handling and cleanup through `delete_due_item`, app-level omission of future timed reminders from the prompt transcript, Python-named active due-item inspection plus active/inactive listing aliases, and file-backed due-item create/update/rename/complete/delete flows including inactive same-name reminder reuse after complete/delete
- `elroy-app` + `elroy-db` + `elroy-agenda`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression for the full Python-style timed-reminder cleanup workflow, proving a due reminder can surface into the live prompt, trigger a model-requested `delete_due_item`, and be removed from both the active DB surface and the markdown file through the real runtime/tool loop instead of only lower-level orchestration tests
- `elroy-app` + `elroy-db` + `elroy-agenda`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression for the Python-style “multiple due reminders surface together” conversation path, not just the lower-level orchestration path
- `elroy-app` + `elroy-db` + `elroy-agenda`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that future timed reminders stay out of the live prompt path, not just the lower-level orchestration path
- `elroy-app` + `elroy-db` + `elroy-agenda`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that a hybrid timed-plus-context reminder still surfaces through the live prompt path when its time component is due, not just the lower-level orchestration path
- `elroy-app`: recall-metadata dedupe for memories and agenda items is now covered both for the normal same-item case and for same-name/different-id reuse, so an inactive old row with a reused logical name does not suppress the newer active replacement during prompt-time memory or contextual reminder recall
- `elroy-app`: context-refresh summary formatting now has direct coverage for Python-closer fast-model prompt construction, including timestamped user/assistant/tool transcript lines, tool call/result wording, and the final `Messages from ... to ...` conversation span used before model-authored summary generation
- `elroy-app`: model-backed semantic recall/reminder selection now has direct regressions that an older but still relevant memory or contextual due item can still surface beyond the earlier tiny recent-candidate fallback, which keeps the bounded Rust selector closer to Python’s whole-store vector recall behavior
- `elroy-app`: model-authored context-refresh summary generation now also has direct coverage that the outgoing summary request includes Python’s explicit `Your word limit is 300. DO NOT EXCEED IT.` instruction instead of only a vague “keep it concise” hint
- `elroy-app`: context refresh now also has end-to-end runtime coverage that `refresh_context_if_needed(...)` persists the live provider’s authored `context_summary` when available and falls back to the deterministic summary when the provider returns empty output
- `elroy-app`: the `AppRuntime::refresh_context_if_needed()` wrapper now also has direct runtime coverage that the shared refresh path persists a model-authored summary when a provider is available, not just the lower helper function
- `elroy-app`: the `AppRuntime::refresh_context_if_needed()` wrapper now also has direct runtime coverage that the shared refresh path can use the Python-style `fast_model` override when `chat_model` points at a different provider, not just the lower helper function
- `elroy-app`: the `AppRuntime::refresh_context_if_needed()` wrapper now also has direct runtime coverage for the empty-summary fallback branch, proving the shared refresh workflow falls back to the deterministic summary when the model returns empty text, not just the lower helper function
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct deferred-refresh regression proving `CliTuiRuntime::refresh_context_if_needed()` actually runs the background task through its normal polling boundary and persists the compressed `context_summary` transcript instead of only surfacing deferred refresh failures
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct deferred-self-reflection regression proving `CliTuiRuntime::run_self_reflection_if_needed()` actually runs the background task through its normal polling boundary and persists the expected self-reflection feature-request artifact instead of only surfacing deferred self-reflection failures
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct deferred-auto-memory regression for the ordinary streamed user-prompt path, proving `start_prompt_stream(...)` queues auto-memory work and the normal polling boundary executes it to a persisted memory file instead of only covering restart/startup stream deferral
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct background-command regression proving `start_command_execution(...)` surfaces `running command...` through the shared footer status path and returns the completed command snapshot through `poll_command_execution()` instead of only relying on TUI fake-runtime tests
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct restart-command regression proving a background `restart_session` command can hand its pending resume prompt back through `take_restart_request()` after command-worker completion instead of only relying on TUI fake-runtime restart coverage
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct restart-stream regression proving the explicit restart resume stream preserves Python’s non-persisted-input contract instead of writing the injected resume prompt back as a new persisted user transcript line
- `elroy-cli` + `elroy-app`: the shared CLI/TUI runtime wrapper now also has a direct startup-resume regression proving the startup resume path routes the resumed startup stream through the same deferred auto-memory queue/polling path, preserves the non-persisted resume-input contract, and consumes the injected resume message only once instead of only covering the explicit restart-stream helper
- `elroy-app`: semantic recall/reminder runtime coverage now also proves the smarter relevance pass is not starved by the older prompt/tool fetch caps, including prompt-time recall of an older relevant memory beyond the previous 50-row memory fetch and `search_memories` surfacing of an older relevant due item beyond the previous `limit * 3` preload
- `elroy-app`: semantic recall/reminder selection now also has direct regressions that an older semantically relevant memory or contextual due item can survive beyond the older 25-candidate relevance-model ceiling, instead of only proving source-fetch widening or tiny recent-fallback rescue
- `elroy-app`: contextual reminder runtime coverage now also proves a due item already pinned in persisted current context is not re-injected by the contextual reminder path, complementing the existing same-turn fast-recall duplicate suppression coverage
- `elroy-app`: the shared runtime now also has a direct two-turn `AppRuntime::process_message(...)` regression that a contextual due item surfaced and persisted on one turn is not re-injected again on a later matching turn, not just helper-level or lower prompt-runtime dedupe coverage
- `elroy-app`: reflective recall now also has prompt-runtime coverage that `reflect=true` can inject model-authored reflective recall content into the real prompt transcript or suppress reflective recall entirely when the relevance model marks it irrelevant, instead of relying only on helper-level reflective payload tests
- `elroy-app`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that ordinary prompt-time fast recall is injected into the live prompt transcript and persisted back as the synthetic `get_fast_recall` tool payload, not just covered in lower-level prompt orchestration or cross-provider `fast_model` override tests
- `elroy-app`: the shared runtime now also has a direct two-turn `AppRuntime::process_message(...)` regression that persisted prompt-time fast recall metadata suppresses duplicate fast-recall reinjection on a later turn, not just helper-level dedupe coverage
- `elroy-app`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that prompt-time fast recall can persist a mixed memory/due-item/plain-agenda `get_fast_recall` payload, not just the memory-only fast-recall case covered below the runtime boundary
- `elroy-app`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that model-authored reflective recall is retained in the persisted transcript with its synthetic recall metadata, not just injected into the live prompt path
- `elroy-app`: the shared runtime now also has a direct `AppRuntime::process_message(...)` regression that irrelevant reflective recall is omitted entirely from both the live-prompt persistence path and the stored transcript, not just from the lower-level prompt-runtime path
- `elroy-app`: the shared runtime now also has a direct two-turn `AppRuntime::process_message(...)` regression that persisted reflective recall metadata suppresses duplicate reflective reinjection on a later turn, not just one-turn reflective persistence/suppression coverage
- `elroy-config` + `elroy-app`: Python-style `fast_model` / `fast_model_api_key` / `fast_model_api_base` config precedence is now covered at config-load level, and the live app provider selection now has focused runtime coverage that background semantic `search_memories` work, prompt-time recall classification through `AppRuntime::process_message(...)`, prompt-time fast-recall semantic selection, prompt-time reflective recall authoring, prompt-time contextual due-item selection, and model-authored context refresh can all use that fast-model config even when `chat_model` points at a different provider
- `elroy-app` + `elroy-db`: current-context memory pinning/drop behavior including same-name cross-store scoping for `add_memory_to_current_context`, exact-name memory read/mutate/source tool scoping to the active configured `memory_dir`, same-name cross-store list/search/examine memory-tool scoping, prompt-time memory-recall scoping to the active configured `memory_dir`, exact-duplicate consolidation scoping to the active configured `memory_dir`, and archived-memory derived-row history for outdated-memory replacement
- `elroy-llm`: stream event model, partial tool-call accumulation, validated context-message rules, provider request payload building including transcript-system-message dedupe for live system prompts, live HTTP client request shaping, non-streaming response parsing, and SSE-backed OpenAI/Anthropic stream parsing
- `elroy-app`: shared runtime behavior for provider config translation, snapshot loading, session-open stale-context pruning, prompt execution, runtime context-load repair of missing/misplaced system messages plus Anthropic-style synthetic first-user insertion, Python-style context-refresh token-budget/compression helpers plus deferred refresh execution and synthetic `context_summary` persistence that now also retains dropped tool-call requests and tool results instead of only user/assistant text, now also carries the Python-style human-readable user-message time range (`Messages from ... to ...`), and now timestamps each rendered summary line instead of leaving the compressed summary text unanchored in time. The refresh path now also has direct helper coverage for formatting conversation text into a Python-closer summary prompt and for converting a non-streaming model completion into the final `Recent conversation summary: ...` string, which is used for best-effort live-provider summary generation before falling back to the deterministic formatter, and direct refresh-path coverage that the stored `context_summary` now still reflects retained recent conversation lines instead of only the dropped prefix. It also covers startup restart/greeting stream handling plus startup-stream auto-memory deferral, Python-style prompt prelude status ordering (`loading context...`, recall status, `thinking...`), Python-style timed-due reminder surfacing with cleanup guidance, the `⏰ DUE ITEM:` prefix, normalized schedule formatting, plus heuristic contextual-trigger surfacing that now persists the matched reminder into current context and avoids duplicating it on later matching turns, and a prompt-runtime regression that hybrid timed-plus-context reminders no longer appear twice when timed-due injection and prompt-time recall/context paths are both eligible in the same turn. Prompt-time memory recall classifier/window controls including Python-style role-prefixed recent context (`user:` / `assistant:`), case-insensitive trivial greeting/acknowledgment skip coverage that now also tolerates surrounding punctuation and pure clarification-only prompts, plus Python-style top-2 injected recalled-memory capping. Prompt-time memory recall now also has direct helper coverage for parsing structured classifier decisions, for turning a non-streaming model completion into a recall yes/no decision, for parsing Python-style structured relevance-filter responses, and for the conservative fallback path when classifier or relevance-filter output is unavailable or invalid. The full prompt runtime now also has coverage that a nontrivial prompt can be classified to skip recall entirely, suppressing synthetic recall injection while still surfacing the `classifying recall...` prelude status, plus a narrower runtime case where a heuristic overlap memory match is filtered back out by the model-backed relevance pass before any synthetic recall context is injected, and now also runtime cases where the same model-backed filter can broaden fast recall beyond zero-overlap heuristic misses by selecting from a bounded recent candidate pool and can prefer semantically relevant fast-recall memory, due-item, and plain-agenda candidates over weaker lexical-overlap matches instead of treating overlap hits as a hard gate. The shared recall helpers now also have direct coverage that the same model-backed relevance pass can both filter memory, due-item, and plain-agenda candidates out of the user-facing `search_memories` / `examine_memories` selection path, broaden those same surfaces beyond overlap-only candidates when wording differs semantically, and prefer semantically relevant candidates over weaker overlap hits there too, and the live tool path now has direct runtime coverage that `search_memories` can prefer semantically relevant memory, due-item, and plain-agenda-item results over weaker lexical-overlap candidates while `examine_memories` can do the same for memory, due-item, and plain-agenda-item results instead of relying only on helper-level selector coverage. The fast prompt-time recall branch now also has direct coverage that it uses the Python-style `get_fast_recall` synthetic tool payload with `recall_metadata` instead of the older Rust-only `search_memories` wrapper shape while now carrying `AgendaItem` metadata too. Persisted current-context `get_fast_recall` payloads for pinned memories, due items, and plain tasks now also have direct coverage for the same `recall_metadata` contract, and prompt-time fast recall now has a regression proving those pinned current-context items are deduped instead of being re-surfaced immediately. Prompt-time memory recall now also has coverage for both the fast `get_fast_recall` synthetic context path and the first `reflect`-gated `get_reflective_recall` synthetic context path, including recall-metadata-based dedupe across transcript-visible recalled memories plus already-in-context reflected due-item and plain-agenda metadata, direct parser coverage for model-authored reflective recall responses, reflective-content formatting that now carries recent conversation lines and the latest user message instead of only raw recalled-memory bullets while also bringing relevant due items plus plain agenda items into the reflective content path, and the model-backed reflective branch that can now replace the deterministic reflective paragraph with explicit JSON-gated `content` or suppress reflective recall entirely when the model marks the recalled context irrelevant. Reflective recall now also has direct coverage that the same model-backed relevance pass can broaden due-item and plain-agenda candidate selection beyond overlap-only wording misses before the reflective content model is asked to author final recall text. It also covers Python-closer user-facing recall-tool capping that now limits `search_memories` / `examine_memories` to 2 memories, 2 due items, and 2 plain agenda items per section even if a larger hidden Rust-side limit is passed. It also covers `search_memories` due-item plus plain-agenda-item inclusion with a Python-closer user-facing search report, explicit memory pin/drop tool behavior for persisted transcript context plus `get_fast_recall` and `get_reflective_recall` acknowledgement tools, formatted `examine_memories` output over matching memories, due items, and plain agenda items with Python-style source-drilldown guidance, a first `get_source_list_for_memory` surface over persisted source metadata that now returns the Python-style bare source list, and a broader `get_source_content_for_memory` path that can now return persisted context-message source content for transcript-derived memories plus archived prior-memory source content for outdated-memory replacements and archived multi-memory source content for consolidated-memory lineage. Memory-backed source content now also includes the archived source memory’s Python-style `#name` fact header instead of only the raw body text. Plain memories without explicit source metadata now return an empty source list and “No sources found…” instead of a raw file fallback, and source-content index errors now use Python-style `Index ... out of range` wording, while `print_memory` now uses the Python `#name` fact-style format and exposes only the Python `memory_name` schema, `print_memories` now lists visible memories oldest-first and exposes only the Python `n` schema field, the active/inactive due-item print-list tools now also expose only the Python `n` schema field, the memory search/examination tools now expose only the Python `query` / `question` schema fields, and the task list tools now expose no Rust-only `limit` parameter, `print_due_item` and the due-item print lists now normalize timed trigger display to Python’s `YYYY-MM-DD HH:MM:SS` format, `create_memory` now exposes only the Python `name`/`text` schema and resets the auto-memory tracker the same way Python’s manual-memory path does, `create_due_item` now exposes only the Python `name`/`text`/`trigger_time`/`trigger_context` schema, `update_due_item_text` now exposes only the Python `name`/`new_text` schema, `rename_due_item` now exposes only the Python `old_name`/`new_name` schema, `rename_task` now exposes only the Python `old_name`/`new_name` schema, source inspection, memory creation confirmation text, due-item print/mutation behavior, task creation context pinning for plain tasks plus explicit triggered-task omission from current context plus inactive same-name task reuse after complete/delete plus refresh/removal across task update/rename/complete/delete plus Python-style `is_active = NULL` persistence for file-backed deleted task rows while completed rows stay inactive `0`, due-item context pinning plus inactive same-name reminder reuse after complete/delete plus refresh/removal across due-item update/rename/complete/delete plus persisted deleted-reminder tombstone surfacing in inactive reminder list/print views after file removal, plain-agenda active-duplicate rejection plus context pinning/refresh/removal across agenda add/update/complete/delete plus list-agenda exclusion of deleted and due-item rows and active-only agenda substring lookup across completed/deleted same-name rows and inactive same-name duplicates during `show_agenda_item` resolution, task mutation missing/duplicate-rename behavior, Python-style `show_task` missing-item wording, Python-style `show_memory` missing-item wording, due-item create/delete fast-recall context side effects, agenda/reminder file deletion semantics, and Python-style agenda creation/mutation argument defaults now use direct Python-style wording/side-effects, including text-derived naming, default-today dates, Python-style `item_date` validation across add/list/list_cmd, Python-style checklist `due_date` validation, Python-style task `item_date` validation on `create_task`, the `item_date` alias, Python-style case-insensitive unique-substring agenda lookup plus the matching no-match/ambiguous-match errors on the agenda-item tool path, `item_name` / `checklist_item_id` / `new_text` agenda mutation aliases, direct Python-style missing-checklist-id wording for agenda checklist edit/complete failures, real timestamp propagation in `add_agenda_item_update`, Python-style `new_text` acceptance on `update_due_item_text`, Python-style `item_date` acceptance on `create_task` plus schema coverage for task trigger/date metadata, duplicate-name rejection for task creation and contextual/timed due-item creation, blank-name rejection for task and due-item creation, missing-trigger rejection for due-item creation, past-trigger rejection for task and timed due-item creation, due-task filtering that excludes future timed plus context-only tasks, and Python-style string line-number coercion on the bounded `read_file` tool, `old_name` rename aliases for tasks and due items, `n` aliases on the Python-style memory/reminder print-list surfaces, `memory_name` aliases on the memory detail and direct update/archive tools, and the Python-style non-error missing-memory response for `update_outdated_or_incorrect_memory`. The runtime now also exposes a test-covered `create_consolidated_memory` tool path that archives named source memories and preserves their lineage metadata, the outdated-memory replacement path now preserves an inactive archived derived row in the rebuilt `memories` table, and the memory tracker now has a first exact-duplicate automatic consolidation pass when the configured threshold is crossed. It also covers Python-style base time/filesystem tools (`get_current_date`, `pwd`, bounded `ls`, bounded `read_file`), Python-style developer commands (`get_help`, `print_config`, `tail_elroy_logs`) with structured plain-text table output for help/config reports including `exclude_tools`, live-registry omission of named tools via Python-style `exclude_tools` config, and the first memory consolidation/recall-quality config rows, Python-named date-scoped `list_agenda_items` plus formatted `list_agenda_items_cmd`, Python-named `restart_session` scheduling when interactive restart support is enabled, Python-closer outdated-memory replacement behavior with source preservation, Python-style `trigger_time` alias plus confirmation strings for due-item creation, Python-style human confirmation text for due-item update/rename/complete/delete, Python-style human confirmation text for agenda add/update/complete/delete and checklist mutations, task mutation confirmation text plus persisted complete/delete closing comments, Python-named print/report formatting for memory and reminder user-facing commands, Python-named memory show/list aliases, archive-memory rebuild semantics that keep `memory_dir/archive` entries out of the active memory surface while retaining inactive derived history rows, post-persist self-reflection triggering plus streamed self-reflection deferral support for TUI-style background scheduling, streamed auto-memory deferral support for both TUI user prompts and startup restart/greeting streams, message-count-driven auto-memory creation, Python-named context reset/refresh tool entries with persisted leading system-message behavior, DB-backed sidebar-detail loading, Python-style agenda-sidebar title formatting and title-to-item resolution, file-backed feature-request sidebar reads/close mutations, Python-style feature-request list/create/edit tool flows with duplicate-aware merge behavior, and file-backed memory/agenda mutation tools
- `elroy-core`: shared turn orchestration, validated transcript repair including wrong-tool-call-id dropping and multi-tool-call retention, force-tool forwarding, Python-style tool-loop status ordering around local tool execution in both buffered and streamed paths, and the shared background-status registry
- `elroy-tools`: canonical tool schema, provider adapter projections, and executable local registry behavior including DB-backed read/write tools in the shared runtime
- `elroy-tui`: layout shell, focus-mode state machine, terminal key mapping, event-loop action transitions, runtime-backed prompt submission, startup restart/greeting stream handling, `restart_session` exit-with-request behavior for CLI-side process restart, Python-style rendered-message bookkeeping after bootstrap/chat turns, chat-mode prompt-history cycling plus draft restoration, chat-mode `Tab` acceptance of runtime-provided plain-agenda completions plus slash-command names, multiline paste flattening in the chat input, wrapped-draft composer growth, mouse-wheel conversation scrolling from chat input while reusing the shared conversation-browse state, zero-argument and fully specified slash-command submit execution plus local slash-command failure handling, basic slash-command modal form launch with positional prefill and submit behavior for underspecified known commands, command-form `name`-field suggestion acceptance on `Tab`, command-form launch-time error retention that keeps the modal open on blocked or immediately failed command launch, command-palette launch-time error retention that keeps the palette open on blocked or immediately failed command launch, local command launch blocking while a prompt stream is active, background local command execution with editable chat input and blocked resubmit while the command worker runs plus restart-request handoff on command completion, `Ctrl+P` command-palette opening plus typed filtering plus first-party system-command surfacing across memories/agenda/improvements/feature-requests/Codex sections plus palette-driven command launch into execute-or-form behavior, command-mode conversation scrolling semantics plus render-time conversation scroll offset, auto-follow of latest conversation output outside intentional history browsing plus re-enable on return to input, draft preservation across browse-mode/sidebar section switches while a stream is active, draft preservation across sidebar/status snapshot updates while a stream is active, post-response/post-cancel focus return to input, deferred context-refresh scheduling, deferred auto-memory scheduling for streamed user prompts, deferred self-reflection scheduling for user prompts with restart-priority short-circuiting, snapshot reload after background-status completion while preserving the local foreground status line, background context polling/rendering rules including mixed interleaved-gap plus trailing-unseen suffix handling, live prompt-stream consumption with blocked resubmit and `Ctrl+C` clear/cancel behavior, idle footer rendering for model/background status, runtime-backed sidebar section switching/opening, detail-modal confirmation behavior for destructive sidebar actions, runtime-backed sidebar mutation actions including feature-request completion, and snapshot rendering
- `elroy-tui`: layout shell, focus-mode state machine, terminal key mapping, event-loop action transitions, runtime-backed prompt submission, startup restart/greeting stream handling, `restart_session` exit-with-request behavior for CLI-side process restart, Python-style rendered-message bookkeeping after bootstrap/chat turns, chat-mode prompt-history cycling plus draft restoration, chat-mode `Tab` acceptance of runtime-provided plain-agenda completions plus slash-command names, multiline paste flattening in the chat input, wrapped-draft composer growth, mouse-wheel conversation scrolling from chat input while reusing the shared conversation-browse state, zero-argument and fully specified slash-command submit execution plus local slash-command failure handling, unknown slash-prefixed input falling back to the ordinary chat-stream path instead of surfacing a local invalid-command error, basic slash-command modal form launch with positional prefill and submit behavior for underspecified known commands, command-form `name`-field suggestion acceptance on `Tab`, command-form launch-time error retention that keeps the modal open on blocked or immediately failed command launch, command-palette launch-time error retention that keeps the palette open on blocked or immediately failed command launch, local command launch blocking while a prompt stream is active, background local command execution with editable chat input and blocked resubmit while the command worker runs plus restart-request handoff on command completion, direct runtime-side failure coverage that a `poll_command_execution()` error clears the running-command state and returns focus to input with `command failed: ...`, plus direct runtime-tick regressions that the loop-side boundary returns `RestartRequested(...)` both when a background `restart_session` command finishes and when a completed prompt stream carries a pending restart request, instead of only exposing lower-level helper results. `Ctrl+P` command-palette opening plus typed filtering plus first-party system-command surfacing across memories/agenda/improvements/feature-requests/Codex sections plus direct palette-driven focus coverage for memories, agenda, improvements, feature-request, and Codex sidebar sections and command launch into execute-or-form behavior, with idle footer coverage that it does not redundantly repeat the command-palette `Ctrl+P` / “commands” hint, sidebar section switches now also resetting selection to the first item instead of carrying a stale index across lists, command-mode conversation scrolling semantics plus render-time conversation scroll offset, auto-follow of latest conversation output outside intentional history browsing plus re-enable on return to input, draft preservation across browse-mode/sidebar section switches while a stream is active, draft preservation across sidebar/status snapshot updates while a stream is active, post-response/post-cancel focus return to input, deferred context-refresh scheduling that now also waits for both active prompt streams and foreground command workflows to finish before starting the background refresh task, deferred auto-memory scheduling for streamed user prompts, deferred self-reflection scheduling for user prompts with restart-priority short-circuiting, direct cancellation coverage that canceled prompt streams do not trigger deferred self-reflection follow-up, snapshot reload after background-status completion while preserving the local foreground status line and now also waiting for both active prompt streams and foreground command workflows to finish before applying the refreshed snapshot, with direct runtime-tick regressions that the delayed refresh still lands once the blocking prompt or command path clears, background context polling/rendering rules including mixed interleaved-gap plus trailing-unseen suffix handling and now also waiting for both active prompt streams and foreground command workflows to finish before appending newly arrived context lines, plus direct coverage that prompt completion makes background context polling immediately eligible instead of delaying new background lines by another full poll interval, live prompt-stream consumption with blocked resubmit and `Ctrl+C` clear/cancel behavior, idle footer rendering for model/background status, runtime-backed sidebar section switching/opening, direct read-only Codex detail-modal coverage that destructive shortcuts are ignored, detail-modal confirmation behavior for destructive sidebar actions, runtime-backed sidebar mutation actions including feature-request completion, and snapshot rendering
- `elroy-app`: runtime snapshot loading now also covers plain-agenda input-completion exposure for the TUI surface, slash-command-name completion exposure from the live tool registry, zero-argument plus fully specified slash-command execution, local rejection of invalid slash commands, basic slash-command form-launch resolution for underspecified known commands, command-form submit execution, Python-style first-pass toast-vs-history result targeting for local command execution, command-form suggestion propagation for `name`-ending parameters from the active plain-agenda completion list, command-palette entry generation including the `/help` alias plus palette-driven zero-arg execution or form launch, and exclusion of triggered tasks from the non-command completion list
- `elroy-cli`: CLI TUI adapter behavior for deferred context-refresh, deferred auto-memory, and deferred self-reflection background worker scheduling plus both running-status and failure surfacing into the shared background-status/footer path, including restart/startup prompt-stream deferred auto-memory queueing and execution through the same polling path, background local command execution with footer-visible `running command...` status plus both success and tool-failure snapshot handoff back into the TUI path, snapshot reload access for post-background TUI refresh, and process re-exec handling of TUI restart requests through `ELROY_RESTART_RESUME_MESSAGE`
- `elroy-cli`: CLI TUI adapter behavior for deferred context-refresh, deferred auto-memory, and deferred self-reflection background worker scheduling plus failure surfacing into the shared background-status/footer path, including restart/startup prompt-stream deferred auto-memory queueing and execution through the same polling path, background local command execution with footer-visible `running command...` status plus completion handoff back into the TUI snapshot path, snapshot reload access for post-background TUI refresh, and direct restart-command construction coverage for the process re-exec handoff through `ELROY_RESTART_RESUME_MESSAGE`
- `elroy-codex` + `elroy-app`: persisted Codex-session upsert/get/list behavior, async dispatch/resume workflow orchestration with isolated git worktrees, background-status registration across running sessions plus the assistant-side completion-follow-up phase, and live session launch/resume/inspection tools
- `elroy-cli` + `elroy-codex` + `elroy-app`: the shared CLI/TUI runtime now also has direct regressions that background `dispatch_codex_session` and `resume_codex_session` commands drive `running command...`, `codex session ... running...`, and `processing codex session ... completion...` through the normal footer/polling boundary before the completed Codex session appears in snapshot/sidebar state
- `elroy-tui` + `elroy-app`: read-only Codex-session sidebar rendering, section switching, and detail opening through the shared runtime, including Python-style detail headings for status/repo/summary/latest agent message
- Direct Python-scenario ports now exist for selected messaging/Codex/UI behaviors, including `persist_input_message=False`, force-tool request plumbing, incremental provider-stream parsing and streamed tool-loop orchestration, startup restart/greeting stream handling, runtime session-open stale-context pruning via `max_context_age_minutes`, persisted leading-system-message behavior for Python-named context reset/refresh and user-preference updates, runtime repair of missing/misplaced system messages before prompt execution and explicit context loads, Anthropic-style synthetic first-user insertion plus snapshot suppression of that line, Python-style context-refresh token-threshold detection, cache-friendly compression ordering/tool-pair preservation, deferred refresh execution, synthetic `context_summary` persistence, deferred context-refresh background scheduling from the CLI TUI adapter plus footer-visible failure surfacing and post-completion snapshot reload, deferred auto-memory omission from streamed prompt finalization plus CLI/TUI-side background scheduling, deferred self-reflection omission from streamed prompt finalization plus CLI/TUI-side background scheduling, TUI restart-request exit behavior plus CLI-side process re-exec through `ELROY_RESTART_RESUME_MESSAGE`, recall-classifier config gating/window behavior, rendered-message bookkeeping after bootstrap/chat turns, background context polling/rendering rules, agenda-sidebar due-label/title-resolution behavior, feature-request duplicate matching plus `list` / `make` / `edit` tool behavior, self-reflection cadence/correction/reopen behavior, message-count-driven auto-memory creation behavior, Python-named context reset tool behavior, feature-request/improvement sidebar visibility and completion behavior, detail-modal confirmation behavior for sidebar actions, TUI draft-editability/cancel behavior during prompt streams, Codex background-status registration plus assistant-side completion-follow-up status persistence, Codex resume/list flows, and Codex sidebar visibility

This is only bootstrap-level coverage. It does not yet prove product parity.
