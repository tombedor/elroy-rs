# Parity Matrix

This document tracks rewrite progress against the Python implementation in `../elroy`.

## Status Vocabulary

- `not started`: no Rust implementation exists yet
- `spike`: exploratory code exists but is not a committed port
- `stubbed`: structure exists but behavior is mostly absent
- `partial`: meaningful behavior is implemented, but parity is incomplete
- `parity`: intended current behavior is implemented and tested
- `intentional delta`: behavior differs on purpose and the delta is documented

## Usage Rules

- Update this file in the same change as status-moving code
- Link tests or note missing coverage when a subsystem moves forward
- Record intentional deltas explicitly instead of hiding them under `partial`

## System-Level Tracking

| Python area | Behavior summary | Rust target | Status | Tests | Notes |
| --- | --- | --- | --- | --- | --- |
| `elroy/__main__.py` | package entrypoint into app | `crates/elroy-cli` and `crates/elroy-app` | `partial` | `crates/elroy-cli` smoke run via `just run` | Current Rust entrypoint performs config load plus DB bootstrap, the shared `elroy-app` runtime now backs live prompt execution and TUI runtime actions, and the `--tui` path now mirrors Python’s restart contract by re-executing the current process with `ELROY_RESTART_RESUME_MESSAGE` when the TUI exits with a pending `restart_session` request. The CLI restart boundary now also has direct coverage that restart command construction preserves the original argv while injecting the Python-style resume env var, and the CLI/TUI runtime wrapper now also has direct startup-resume regressions proving the resumed startup-stream path runs through the normal deferred auto-memory queue/polling boundary, preserves the non-persisted resume-input contract, and consumes the injected resume message only once instead of only relying on the explicit restart-stream helper |
| `elroy/config/` | env/config-file/model config resolution | `crates/elroy-config` | `partial` | `crates/elroy-config/src/lib.rs` | Defaults, YAML loading, env override precedence, the Python-style base-tool config surface (`include_base_tools` plus `exclude_tools`), greeting/bootstrap context controls (`enable_assistant_greeting`, `min_convo_age_for_greeting_minutes`, `max_context_age_minutes`), Python-style context-refresh token controls (`max_tokens` with derived `context_refresh_target_tokens`), message-cadence config for both self-reflection and auto-memory creation (`messages_between_self_reflection`, `messages_between_memory`), and the first memory config surface around consolidation/recall quality (`memories_between_consolidation`, `l2_memory_relevance_distance_threshold`, `memory_cluster_similarity_threshold`, `max_memory_cluster_size`, `min_memory_cluster_size`, `reflect`) now exists. The Python-style `fast_model` / `fast_model_api_key` / `fast_model_api_base` background-model surface now also exists with file/env precedence, falling back to `chat_model` when unset; live runtime coverage now proves that cross-provider fast-model override on prompt-time recall classification, prompt-time fast-recall semantic selection, prompt-time reflective recall authoring, prompt-time contextual due-item selection, background semantic recall selection, and context refresh summary generation. The `reflect` flag is now also consulted by prompt-time memory recall to switch between the fast synthetic `search_memories` context and a first reflective-recall context path. Model/config surface is still narrow, though prompt execution now consults persisted assistant naming on top of config defaults |
| `elroy/core/` | session, turn, runtime, orchestration | `crates/elroy-core` | `partial` | `crates/elroy-core/src/lib.rs` | Session/turn boundaries exist, the orchestrator now supports both batch turns and stream-oriented tool loops while accumulating normalized transcript messages, and a live provider adapter can drive real provider calls; transcript repair for broken assistant/tool message sequences now exists, but higher-level repository composition and recall workflow parity are still missing |
| `elroy/llm/` | model client, parsing, stream handling | `crates/elroy-llm` | `partial` | `crates/elroy-llm/src/lib.rs` | Provider-neutral stream events, partial tool-call accumulation, validated context-message modeling, request builders, and live HTTP clients for OpenAI/Anthropic exist; OpenAI Responses and Anthropic Messages now both have SSE-backed streaming paths, but richer parser coverage and broader runtime integration are still missing |
| `elroy/tools` | tool schema, registration, execution | `crates/elroy-tools` and `crates/elroy-app` | `partial` | `crates/elroy-tools/src/lib.rs`, `crates/elroy-app/src/lib.rs` | Canonical schema plus OpenAI/Anthropic adapters exist, the shared runtime now has executable tools for Python-style base time/filesystem utilities (`get_current_date`, `pwd`, bounded `ls`, bounded `read_file`), developer commands (`get_help`, `print_config`, `tail_elroy_logs`) with structured plain-text table output closer to the Python `rich` reports, including the first memory consolidation/recall-quality config fields in `print_config`. Memory/agenda listing including Python-named date-scoped `list_agenda_items` plus formatted `list_agenda_items_cmd`, exact detail lookup including Python-named `show`/`print` aliases for active memories and due items plus active/inactive due-item listing aliases, and Python-closer print/report formatting for `print_memory`, `print_memories`, `search_memories`, `print_due_item`, `print_active_due_items`, and `print_inactive_due_items` while the parallel `show`/`list` surfaces stay structured. The `print_memory` path now also matches Python’s `#name` fact-style body format and advertises only Python’s `memory_name` schema, the `print_memories` path now follows Python’s oldest-first visible ordering and advertises only Python’s `n` schema field, the `search_memories` and `examine_memories` tools now advertise only Python’s `query` / `question` schema fields rather than the older Rust `limit` field, the task list tools now advertise no schema fields rather than the older Rust `limit` field, the `print_due_item` plus active/inactive due-item print-list paths now normalize timed trigger display to Python’s `YYYY-MM-DD HH:MM:SS` format, the memory and due-item print-list surfaces now also accept Python’s `n` argument alias alongside `limit`, and the active/inactive due-item print-list tools now advertise only Python’s `n` schema field rather than the older Rust `limit` alias. The memory detail/mutation tools now accept Python’s `memory_name` argument alongside the earlier Rust `name` alias, `create_memory` now exposes the Python-style narrow `name`/`text` schema instead of leaking unrelated agenda-style fields, `create_due_item` now exposes only the Python `name`/`text`/`trigger_time`/`trigger_context` schema while still accepting Rust-side aliases internally, `update_due_item_text` now advertises only the Python `name`/`new_text` schema while still accepting the old Rust `text` alias internally, `rename_due_item` now advertises only the Python `old_name`/`new_name` schema while still accepting the old Rust `name` alias internally, and `rename_task` now advertises only the Python `old_name`/`new_name` schema while still accepting the old Rust `name` alias internally. The runtime also has file-backed memory creation with the Python-style `New memory created: ...` confirmation string, file-backed memory updates/archive, and the Python-style `update_outdated_or_incorrect_memory` flow now preserves both the source-lineage replacement behavior and the plain non-error `Memory '<name>' not found` response when the target memory is absent. It also has a first runtime `create_consolidated_memory` tool path that archives named source memories and preserves multi-memory source metadata for lineage inspection. It also has explicit memory pin/drop into the persisted transcript context, the Python-style no-op `get_fast_recall` acknowledgement tool, a first Python-style no-op `get_reflective_recall` acknowledgement tool for synthetic reflective recall context, a first formatted `examine_memories` question-answer surface over memories, due items, and plain agenda items that now includes Python-style source-drilldown guidance, a first Python-named `get_source_list_for_memory` tool over persisted source metadata that now returns the Python-style bare source list instead of a Rust wrapper object, and a broader `get_source_content_for_memory` path that now returns stored context-message source content for transcript-derived memories, archived prior-memory source content for outdated-memory replacements, or archived multi-memory source content for consolidated-memory lineage. Memory inspection/source tools now also use Python-style missing-memory wording plus Python-style `Index ... out of range` casing, and plain memories without explicit source metadata correctly report no sources instead of falling back to the memory file body. Reminder creation now accepts the Python-style `trigger_time` alias and returns Python-style contextual/timed/hybrid confirmation text, `update_due_item_text` now also accepts Python’s `new_text` argument name alongside the older Rust `text` alias, due-item deletion now persists optional `closing_comment` metadata, and the bounded `read_file` tool now also coerces Python-style string line numbers for `start_line` / `end_line` instead of only accepting numeric JSON values. Agenda creation now also accepts the Python-style text-only shape, understands the Python-style `item_date` argument name alongside the older Rust `date` alias, derives the item name from the first text line when `name` is omitted, and defaults undated items to today instead of `unscheduled`. Agenda lookup for the agenda-item tool path now also accepts Python-style case-insensitive unique substring matches over active items, with matching `No agenda item found matching ...` and `Multiple agenda items match ... Be more specific.` errors. The agenda mutation tools now also accept Python-style `item_name`, `checklist_item_id`, and `new_text` argument names alongside the earlier Rust aliases, and checklist edit/complete missing-id failures now use the direct Python-style `Agenda item '<name>' has no checklist item <id>.` wording instead of a wrapped generic mutation error. Task and due-item rename tools now also accept Python-style `old_name` alongside the earlier Rust `name` alias. Agenda update/complete/delete flows now use Python-style confirmation text including the real appended update timestamp, Python-style feature-request `list` / `make` / `edit` operations with duplicate-aware merge behavior and lightweight provenance capture, Python-named context reset/refresh tool entries (`reset_messages`, `refresh_system_instructions`) that now persist a first-class system message instead of acting as no-ops/clear-all shims, and a first `restart_session` tool that schedules an interactive restart when the runtime enables restart support. Config can now disable the base tool surface entirely via `include_base_tools` or exclude named tools from the live registry via `exclude_tools`; broader tool coverage/write workflows are still missing |
| `elroy/db/` | DB manager, sessions, migrations | `crates/elroy-db` | `partial` | `crates/elroy-db/src/lib.rs` | SQLite connection, migrations, persisted bootstrap inventory, and derived memory/agenda tables rebuilt from files exist; full domain schema and higher-level repository workflows are not implemented |
| `elroy/repository/context_messages/` | context read/refresh workflow | `crates/elroy-db`, `crates/elroy-core`, and `crates/elroy-app` | `partial` | `crates/elroy-db/src/lib.rs`, `crates/elroy-core/src/lib.rs`, `crates/elroy-app/src/lib.rs` | SQLite-backed context-message persistence and transcript continuation exist, including a more race-tolerant `context_message_sets` get-or-create path that now uses insert-or-ignore plus re-read behavior instead of assuming the insert always wins. The shared runtime now reloads/persists transcript state for both CLI prompt execution and TUI submission, transcript loading repairs orphaned or incomplete assistant/tool-call sequences before prompt execution, runtime session-open now ports the Python stale-context drop behavior via `max_context_age_minutes` while preserving a leading non-system opener, and Python-named `refresh_system_instructions` / `reset_messages` flows now persist or replace a first-class leading system message while user-preference mutations refresh that stored system message. Runtime context loads and prompt execution now also repair missing/misplaced system messages before use, and for Anthropic-style alternating-role providers they insert the synthetic first user message when the transcript begins with assistant output; repaired transcripts are persisted back to SQLite, while snapshot rendering suppresses that synthetic user line. Python-style `is_context_refresh_needed` and cache-friendly context compression helpers now exist with token-budget and tool-pair preservation tests, and the shared runtime now exposes a deferred refresh path that compresses persisted transcript state, snapshots a conversation memory, and appends a synthetic `context_summary` tool/result pair when the threshold is exceeded. That synthetic summary now retains dropped tool-call requests and tool results instead of flattening refresh context down to user/assistant text only, now carries the Python-style human-readable user-message time range (`Messages from ... to ...`) instead of omitting the conversation span entirely, now timestamps each rendered summary line instead of emitting unanchored role-only text, and now summarizes the full pre-refresh conversation instead of only the dropped prefix. Context refresh can now also attempt a Python-closer model-authored summary over formatted conversation text when a live provider is available, while falling back to the deterministic formatter when provider config or completion fails, and the real `refresh_context_if_needed(...)` path now has end-to-end coverage for both of those branches instead of only helper-level summary tests. Those background summary attempts now also honor the Python-style `fast_model` config surface when it is configured instead of being forced onto the main chat model, and both the helper path plus the `AppRuntime` wrapper now have direct regressions for the normal model-authored-summary path, the cross-provider fast-model override, and the empty-model-output fallback branch rather than only tool-surface evidence. The CLI/TUI runtime wrapper now also has a direct deferred-refresh regression proving `CliTuiRuntime::refresh_context_if_needed()` actually runs that background refresh through its normal polling boundary and persists the compressed `context_summary` transcript instead of only surfacing failure status. Prompt finalization now triggers both the Python-style self-reflection orchestrator and a first message-count-driven auto-memory creation path against the persisted transcript, and the CLI/TUI runtime wrapper now also has a direct deferred-self-reflection regression proving `CliTuiRuntime::run_self_reflection_if_needed()` actually runs that background task through its normal polling boundary and persists the expected self-reflection feature-request artifact instead of only surfacing failure status. The same CLI/TUI runtime wrapper now also has a direct deferred-auto-memory regression for the ordinary streamed user-prompt path, proving the normal `start_prompt_stream(...)` boundary queues auto-memory work and the same polling path executes it to a persisted memory artifact instead of only covering restart/startup stream deferral. Both streamed TUI user prompts and startup restart/greeting streams can now defer that auto-memory work into the CLI background worker/footer model instead of blocking finalization inline. The remaining gap is the fuller Python refresh workflow around consistently LLM-generated summary quality and broader background scheduling/orchestration parity |
| `elroy/repository/memories/` | memory lifecycle, recall, consolidation | `crates/elroy-memory` | `partial` | `crates/elroy-memory/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-db/src/lib.rs` | File-backed memory creation, update, archive, and filename/path handling now live in a dedicated crate and are exercised through the shared runtime; bootstrap discovery still skips `memory_dir/archive` for the active memory surface, but archived memory files are now also discovered separately and synced into the derived `memories` table as inactive rows, so outdated-memory replacement preserves Python-style historical versions instead of dropping them entirely during rebuild. Transcript-derived memory creation now also records context-message source ids in memory-file frontmatter, `get_source_list_for_memory` can expose that persisted source metadata using the Python-style bare source-list shape, and `get_source_content_for_memory` can replay those source messages instead of only showing the memory file body. Memory-backed source inspection now also renders archived source memories with their Python-style `#name` fact header, so consolidated-memory and outdated-memory source drilldown includes the source memory identity instead of only the raw body text. The outdated-memory update path now also replaces the active memory with a new file-backed version that preserves the archived prior memory as explicit source metadata, and now also matches Python’s non-error `Memory '<name>' not found` behavior when the target memory is absent. The runtime now exposes a first explicit `create_consolidated_memory` tool path that archives multiple named source memories and preserves them as retrievable source metadata, so source inspection covers context-message lineage plus first memory-to-memory and consolidated-memory lineage. Plain memories without explicit source metadata now correctly return an empty source list and “No sources found…” the way Python does, source-content index errors now use Python-style `Index ... out of range` wording, `print_memory` now matches Python’s non-error missing-memory message, `show_memory` now uses the Python-style `Memory '<name>' not found for the current user.` wording, `print_memories` now accepts Python’s `n` alias, the memory detail and direct mutation/archive tools now also accept Python’s `memory_name` argument, and `examine_memories` now includes Python-style guidance for drilling into source content. Prompt finalization now also persists a first message-count-driven auto-memory tracker and creates deterministic file-backed conversation memory snapshots when the configured threshold is crossed, resetting that tracker on memory creation, and the tracker now performs a first automatic consolidation pass that collapses exact-duplicate active memories into a single consolidated memory when the configured threshold is reached. Prompt-time memory recall now uses conservative greeting/ack skip heuristics, recent transcript context, dedupe against transcript-visible recalled memories, and token-overlap matching over active memories, and it now consults the Rust `reflect` flag to switch between the existing fast synthetic `search_memories` context and a first synthetic `get_reflective_recall` context payload with recall metadata. That reflective branch now carries recent conversation context plus the latest user message in its reflective content, now also includes relevant due items plus plain agenda items alongside memories the way Python’s reflective-recall orchestrator does, now suppresses already-in-context reflected due items and agenda items in addition to already-in-context memories, and can now use model-authored reflective content with explicit `is_relevant` gating when a non-streaming provider model is available while falling back to the earlier deterministic reflective text on parse or completion failure. The live `search_memories` tool still reports overlapping due items plus plain agenda items in a Python-closer user-facing search view. Python-named live tools can explicitly pin/drop one active memory in the persisted transcript context, and current-context pinning now has explicit same-name cross-store scoping coverage so another configured memory directory cannot leak the wrong memory body into the local transcript. The exact-name memory tool surface now also scopes read/mutate/archive/source operations to the active configured `memory_dir`, so same-name memories from another configured store cannot leak through `show_memory`, `print_memory`, source inspection, or direct file-backed mutations. The broader memory query surface now also scopes list/search/examine, prompt-time recall injection, sidebar memory-title loading, and exact-duplicate consolidation to the active configured `memory_dir`, so another configured store cannot leak memories into user-visible query/report/consolidation paths or into transient recall context. A first `examine_memories` tool surface now formats matching memories, due items, and plain agenda items for the model. Classifier parity, full reflective-recall quality, richer source metadata/inspection beyond these archived-path lineages, LLM-based memory formulation quality, embedding-based consolidation clustering, and broader lifecycle workflows are still missing |
| `elroy/repository/reminders/` | reminders and due-item logic | `crates/elroy-app`, `crates/elroy-db`, and `crates/elroy-agenda` | `partial` | `crates/elroy-app/src/lib.rs`, `crates/elroy-db/src/lib.rs`, `crates/elroy-agenda/src/lib.rs` | Active due-item listing plus file-backed due-item create/update/rename/complete/delete flows now exist on top of agenda-backed trigger fields, and the live tool surface now includes Python-named active due-item inspection plus active/inactive print formatting and inactive due-item listing. Due-item creation now accepts the Python-style `trigger_time` alias, rejects blank names with the Python-style `Due item name cannot be empty` error, rejects missing triggers with the Python-style `Either trigger_time or trigger_context must be provided for due items` error, rejects past timed triggers with the Python-style “Attempted to create a due item … which is in the past” error, returns the Python contextual/timed/hybrid confirmation strings, rejects duplicate active names with Python-style `Timed due item '...' already exists` / `Contextual due item '...' already exists` errors, and persists a Python-style `get_fast_recall` synthetic context entry for the created reminder. Due-item update/rename/complete/delete now return Python-style human confirmation text plus direct reminder-specific missing/duplicate errors, the rename path now also accepts Python’s `old_name` argument, `update_due_item_text` now also accepts Python’s `new_text` argument name, and the current Rust-side reminder context surface now stays coherent across due-item update/rename/complete/delete by refreshing or removing that pinned `get_fast_recall` context after mutation. `print_due_item` now also matches Python’s missing-item error wording by listing valid active due items, the active/inactive print list tools now also accept Python’s `n` alias, and inactive due items now also have direct coverage that the same reminder name can be recreated after complete/delete while preserving the logical active due-item name. Deleted due items are now also persisted into a dedicated inactive-history tombstone surface so they continue to appear in inactive due-item listings with their closing comment after the underlying file has been removed, matching Python’s inactive reminder behavior more closely. Python-style timed-due items are now surfaced into prompt context with the Python `⏰ DUE ITEM:` prefix, normalized `YYYY-MM-DD HH:MM:SS` schedule formatting, explicit cleanup guidance before model execution, and app-level prompt/runtime coverage that surfaced due items can appear together, surface when hybrid time-based reminders are due, surface when contextual trigger text overlaps, be handled and deleted through the shared messenger/tool loop, and stay absent when still in the future. Context-triggered due items now also reuse the persisted `get_fast_recall` due-item context format when first recalled at prompt time, so later matching turns do not duplicate the same reminder context in the transcript. Dedicated reminder-domain crate and richer recall behavior are still missing |
| `elroy/repository/agenda/` | agenda item workflows | `crates/elroy-agenda` | `partial` | `crates/elroy-agenda/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-db/src/lib.rs` | File-backed agenda creation, update-log appends, completion, deletion, checklist add/edit/complete flows, an agenda-only query view that excludes reminder-triggered items, and Python-named date-scoped `list_agenda_items` plus formatted `list_agenda_items_cmd` tools now exist. The agenda mutation tools now also return Python-style human confirmation text for add/update/complete/delete and checklist mutations rather than generic JSON payloads, `add_agenda_item` now supports Python’s `item_date` argument name plus text-derived naming and today-by-default date behavior, validates `item_date` with the same Python-style `Invalid date format '...'. Use YYYY-MM-DD.` error across add/list/list_cmd, rejects active duplicate names with the inherited task-style `Task '...' already exists` error, and the plain-agenda Rust tool path now participates in the existing task-context surface by pinning newly created plain agenda items into current context, refreshing that pinned context after `add_agenda_item_update`, and removing it after complete/delete. Checklist addition now also validates optional `due_date` with the Python-style `Invalid due_date format '...'. Use YYYY-MM-DD.` error. The date-scoped agenda list path now also has direct coverage that deleted agenda items and due-item rows stay out of `list_agenda_items`, and agenda substring lookup now has direct coverage that completed/deleted same-name rows are ignored, matching the Python agenda tests more closely. The other agenda mutation tools now accept Python-style `item_name` / `checklist_item_id` / `new_text` argument names alongside the earlier Rust aliases, and agenda-item deletion now unlinks the underlying file the way Python does. Richer edits and broader workflow parity are still missing |
| `elroy/repository/tasks/` | task mutation workflows | `crates/elroy-tasks` and `crates/elroy-app` | `partial` | `crates/elroy-tasks/src/lib.rs`, `crates/elroy-app/src/lib.rs` | A dedicated task crate now wraps unified agenda-backed task file mutations and task-oriented queries for active/triggered/due/today task views, and the live runtime now exposes task create/show/list/update/rename/complete/delete tools with optional date and trigger metadata on creation. `create_task` now also advertises that metadata in its tool schema, accepts the Python-style `item_date` alias alongside the earlier Rust `date` field, validates task dates with the Python-style `Invalid date format '...'. Use YYYY-MM-DD.` error, rejects blank names with the Python-style `Task name cannot be empty` error, rejects past trigger datetimes with the same Python-style “Attempted to create a due item … which is in the past” error path, rejects duplicate active names with the Python-style `Task '...' already exists` error instead of silently uniquifying filenames, and `show_task` now uses the same Python-style `Active task '...' not found.` wording as the other task mutation lookups. The due-task query path now directly excludes future timed tasks plus context-only tasks the way Python’s `get_due_tasks` logic does. Plain task creation now also persists a Python-style `get_fast_recall` task context entry so newly created non-triggered tasks enter current context immediately, while triggered tasks now have direct coverage that they stay out of current context the way the Python task orchestrator’s plain-task-only bridge does, inactive tasks now also have direct coverage that the same task name can be recreated after complete/delete while preserving the logical active task name, and file-backed deleted task rows now preserve Python-style `is_active = NULL` semantics in the derived table while completed rows stay `is_active = 0`. The current Rust-side task context surface now stays coherent across task update/rename/complete/delete by refreshing or removing that pinned context after mutation. The task mutation tool surface now also returns human confirmation text, uses direct Python-style active-task missing/duplicate rename errors, accepts Python’s `old_name` rename argument alongside the earlier Rust alias, and persists optional delete/complete closing comments rather than generic JSON mutation payloads. Recall-index parity and broader orchestrator side effects are still missing |
| `elroy/repository/feature_requests/` | markdown-backed feature request storage and queries | `crates/elroy-feature-requests` and `crates/elroy-app` | `partial` | `crates/elroy-feature-requests/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-tui/src/lib.rs` | A dedicated feature-request crate now covers markdown-backed create/load/list/update flows, self-reflection-vs-user-request source inference, identifier lookup, duplicate matching, active-only improvement queries, and sidebar-driven close mutations. The shared runtime now exposes Python-style `list_feature_requests`, `make_feature_request`, and `edit_feature_request` live tools, and the shared runtime/TUI can render `Improvements` and `Feature Requests` sidebar sections, open Python-style detail modals, and close active requests from the modal. Feature-request file/query parity is now broader; remaining gaps are in the wider context-refresh/background-thread workflow rather than the feature-request surface itself |
| `elroy/repository/self_reflection/` | correction-driven background product reflection | `crates/elroy-self-reflection` and `crates/elroy-app` | `partial` | `crates/elroy-self-reflection/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-config/src/lib.rs` | A dedicated self-reflection orchestrator crate now ports the Python cadence gate, correction-phrase proposal generation, feature-request create/reopen/dedupe behavior, and explicit-zero disable semantics. The shared prompt runtime still supports inline post-persist self-reflection for non-TUI flows, and the TUI path now defers user-prompt self-reflection into a background worker so the shared footer/status model can surface `reflecting on recent conversation...` and the sidebar can refresh after completion. That closes the first Python-style background scheduling slice for reflection, but the broader context-refresh/background-thread orchestration path is still missing |
| `elroy/repository/user/` | user session and preferences | `crates/elroy-user`, `crates/elroy-db`, and `crates/elroy-app` | `partial` | `crates/elroy-user/src/lib.rs`, `crates/elroy-db/src/lib.rs`, `crates/elroy-app/src/lib.rs` | SQLite-backed persisted assistant name, preferred name, full name, and persona now exist for the local user; live tools can set/get names, update/reset persona, prompt execution now renders the Python persona template with persisted assistant/user naming, the preferred/full-name tools now honor Python-style `override_existing` semantics and messages, and preference mutations now refresh the persisted leading system message used by the context-message tool surface. Broader session identity behavior is still missing |
| `elroy/repository/codex_sessions/` | Codex session persistence and workflows | `crates/elroy-codex`, `crates/elroy-db`, and `crates/elroy-app` | `partial` | `crates/elroy-codex/src/lib.rs`, `crates/elroy-app/src/lib.rs` | SQLite-backed Codex session persistence now exists with per-user upsert/get/list behavior, async dispatch/resume flows that manage isolated git worktrees and merge into the long-lived `agent` branch, Python-style recursive session-file discovery under `~/.codex/sessions`, background-status registration across running Codex sessions, and background completion follow-up turns that reuse the shared Rust conversation/transcript pipeline with `persist_input_message=false`; live tools can launch, resume, list, and inspect sessions, and the shared runtime/TUI can surface recent Codex sessions read-only with Python-style detail headings for status/repo/summary/latest agent message, but broader interactive UI workflow parity is still missing |
| `elroy/messenger/` | stream/event messaging loop | `crates/elroy-app` and `crates/elroy-core` | `partial` | `crates/elroy-app/src/lib.rs`, `crates/elroy-core/src/lib.rs`, `crates/elroy-llm/src/lib.rs` | A messenger-style shared runtime surface now exists via `AppRuntime::process_message` and a lazy `process_message_stream`, which wrap the core orchestrator path, support user/system role persistence, tool enable/disable, `persist_input_message=false`, named `force_tool` selection forwarded into provider-specific tool-choice requests, and real incremental live-provider streaming before transcript persistence finalization. The shared runtime now emits Python-style prelude statuses for `loading context...`, recall classification/fetch when applicable, due-item surfacing, and `thinking...` before model output, and the shared core tool loop now also emits `running <tool>...` and post-tool `thinking...` statuses around local tool execution. Prompt-time memory recall also respects Python-style classifier enable/disable and recent-message window config. The same runtime path is reused for foreground prompts and background Codex follow-up turns. The remaining gap is broader UI-side live streaming behavior rather than the shared messenger/runtime iterator path |
| `elroy/ui` | Textual TUI | `crates/elroy-tui` | `partial` | `crates/elroy-tui/src/lib.rs`, `crates/elroy-cli/src/main.rs`, `crates/elroy-app/src/lib.rs` | `ratatui` shell, focus/key-mode logic, and a runnable terminal loop exist; the TUI now consumes the shared runtime's lazy prompt stream incrementally, keeps draft input editable while a prompt is running, now records successful prompts into chat-mode input history with Python-style `Up`/`Down` recall plus draft restoration, accepts `Tab` completion against runtime-provided active plain-agenda suggestions plus slash-command names, and now executes both zero-argument and fully specified slash commands locally through the shared runtime instead of sending them to the model. Invalid slash commands fail locally, and underspecified known slash commands now open a basic modal command form with positional prefill so missing values can be filled without falling back into chat; those forms now also carry Python-style `name`-field suggestions from the active plain-agenda completion list and let `Tab` accept a matching suggestion before falling back to field navigation. Launch-time command-form errors now stay attached to the open modal instead of closing the form before surfacing the failure, including the “current task already running” case, and command-palette launches now keep the palette open on the same class of immediate launch failures instead of collapsing the modal first. Local command launches are also now blocked while a chat stream is active, matching Python’s shared “current task” guard instead of letting palette/slash/form actions race a live prompt turn. The shared local-command runtime now also has a first Python-style result-target split: short single-line success results for toast-class commands like `create_memory` stay in transient status feedback instead of always being appended to conversation history, while history-target commands like `/help` still write their output into the conversation pane. The chat input now also handles terminal paste events directly, flattening multiline pasted text into a single prompt line instead of dropping raw newlines into the composer, and the composer now grows vertically for wrapped drafts instead of pinning all input to a single fixed row. A first command-palette path now exists too: `Ctrl+P` opens a modal command list, typing filters the list against titles/descriptions, first-party system commands now cover all Rust sidebar sections (`Focus Memories`, `Focus Agenda`, `Focus Improvements`, `Focus Feature Requests`, `Focus Codex Sessions`) alongside `Refresh System Instructions` and `Reset Messages`, zero-arg/system commands execute immediately, and parameterized commands route into the same modal command form path. Palette-driven sidebar focus now also has direct regressions for both feature requests and Codex sessions. Those local command actions now execute through a background worker path instead of blocking inline, so chat input stays editable, resubmits are blocked until completion, the footer can surface `running command...` until the completed snapshot is applied, and command-triggered `restart_session` requests now also propagate back to the CLI process-restart boundary instead of stopping at snapshot application. The CLI-side runtime wrapper now also has direct regressions proving that background command-worker path really surfaces `running command...`, hands the completed snapshot back through `poll_command_execution()`, preserves tool-layer slash-command failures as completed snapshots instead of collapsing them into generic worker errors, and can return a pending restart resume prompt through `take_restart_request()` after a background `restart_session` command instead of only relying on TUI fake-runtime tests. Command-mode conversation focus now also has its own scroll state, so `j`/`k` and arrow movement scroll conversation history instead of incorrectly mutating sidebar selection when the history pane is active, while ordinary prompt/command/background output now auto-follows the latest conversation lines by default until the user intentionally scrolls away in history mode and returns to input. Mouse-wheel scrolling now also moves that conversation history from the chat input and conversation pane instead of requiring explicit browse mode first. It restores focus to the chat input after a response finishes or is cancelled, blocks resubmits until completion, supports `Ctrl+C` clear/cancel behavior, can run startup restart/greeting streams without rendering a synthetic user line, and now returns a pending `restart_session` request to the CLI boundary so the process can be re-executed with the Python-style resume prompt instead of faking restart in-process. It tracks rendered context-message ids with the same post-bootstrap/post-chat anchoring rules as the Python app, schedules a deferred context-refresh pass after completed prompt streams, now dispatches that deferred refresh through a background CLI-side worker so the footer can keep rendering while refresh runs, also defers user-prompt self-reflection into that post-turn background path while correctly letting a pending restart short-circuit later post-turn background scheduling, reloads the runtime snapshot once background work clears so sidebar/context-derived state does not stay stale until the next foreground action without clobbering the local foreground status line, polls persisted context messages to append newly arrived trailing background updates while suppressing ambiguous interleaved duplicates and now also preserves a truly new trailing suffix even when earlier unseen interleaved context messages exist, and prompt completion now makes that background context polling immediately eligible instead of delaying new background lines by another full poll interval. It now mirrors the Python idle footer model by showing `● <model>` plus background activity when no foreground prompt is active. The sidebar now includes Python-style `Improvements` and `Feature Requests` sections in addition to agenda and read-only Codex sessions, with section switching, open behavior, and detail-modal close/complete semantics for active feature requests. Codex detail modals now also have direct coverage that destructive shortcuts are ignored and that the rendered detail content uses Python-style status/repo/summary/latest-agent-message headings, preserving the intended read-only sidebar behavior. The agenda sidebar still uses active-task data rather than plain-agenda-only rows, including Python-style trigger-time formatting and `(Due)` suffixes for overdue timed items, and opened sidebar items use a detail modal with Python-style close keys plus destructive-action confirmation before archive/delete. Broader background-status producer coverage, fuller Textual-style command-palette system-command behavior, and remaining session workflows are still missing |
| `tests/` | Python behavioral coverage | Rust test suites | `partial` | `crates/elroy-app/src/lib.rs`, `crates/elroy-config/src/lib.rs`, `crates/elroy-core/src/lib.rs`, `crates/elroy-codex/src/lib.rs`, `crates/elroy-db/src/lib.rs`, `crates/elroy-feature-requests/src/lib.rs`, `crates/elroy-llm/src/lib.rs`, `crates/elroy-self-reflection/src/lib.rs`, `crates/elroy-tui/src/lib.rs`, `crates/elroy-cli/src/main.rs` | Rust test suites now directly cover a first set of Python parity scenarios, including non-persisted messenger input, force-tool request plumbing, incremental provider-stream parsing and streamed tool-loop orchestration, startup restart/greeting stream handling plus CLI-side deferred startup/restart auto-memory queueing and execution, rendered-message bookkeeping after bootstrap/chat turns, background context polling/rendering rules, TUI live prompt streaming/editability/cancel behavior, prompt-history up/down cycling plus draft restoration plus chat-mode `Tab` completion over runtime-provided plain-agenda suggestions, multiline paste flattening in the chat input, mouse-wheel conversation scrolling from chat input, browse-mode draft preservation during active streams, draft preservation across sidebar/status snapshot updates during active streams, local command launch blocking during active prompt streams plus command-palette/form retention on that failure path, slash-command local execution plus modal form launch plus background command-worker completion, command-palette opening plus typed filtering plus first-party system-command surfacing plus palette-driven command execution/form launch, command-form `name`-field suggestion propagation plus `Tab` acceptance, command-mode conversation scrolling semantics plus render-time conversation scroll offset, feature-request/improvement sidebar visibility and close behavior, feature-request duplicate matching plus `list` / `make` / `edit` tool flows, self-reflection cadence/correction/reopen/dedupe behavior plus deferred streamed self-reflection omission for TUI-style post-turn scheduling, message-count-driven auto-memory creation plus tracker persistence including manual-memory reset behavior, transcript-validation repair scenarios including wrong tool-call ids and multi-tool-call retention, due-item prompt-context generation for multiple simultaneous timed reminders plus app-level prompt/runtime surfacing, contextual-trigger surfacing, hybrid timed-reminder surfacing, contextual reminder context pinning plus inactive same-name reminder reuse after complete/delete plus later-turn dedupe plus refresh/removal across due-item update/rename/complete/delete, task context pinning on creation plus explicit triggered-task omission from current context plus inactive same-name task reuse after complete/delete plus refresh/removal across task update/rename/complete/delete, plain-agenda active-duplicate rejection plus context pinning/refresh/removal across agenda add/update/complete/delete plus agenda-list exclusion of deleted and due-item rows and active-only substring lookup across completed/deleted same-name rows and inactive same-name duplicates during `show_agenda_item` resolution, and future-reminder omission, same-name cross-store current-context memory pinning, same-name cross-store exact-name memory read/mutate/source scoping, same-name cross-store memory query-tool scoping, same-name cross-store prompt-time recall scoping, same-name cross-store exact-duplicate consolidation scoping, archived-memory derived-row history for outdated-memory replacement, memory-backed source-content rendering that includes archived source memory identity, case-insensitive trivial recall-skip heuristics for greetings/acknowledgments, Python-named context reset tool behavior, Codex async dispatch/resume persistence plus completion-follow-up status visibility, and Codex/TUI sidebar visibility. Broad fixture reuse and end-to-end parity coverage are still missing |

## Cross-Cutting Parity Requirements

These are not single modules, but they must be tracked during implementation:

| Requirement | Status | Tests | Notes |
| --- | --- | --- | --- |
| Config precedence and defaults | `partial` | `crates/elroy-config/src/lib.rs` | Default config values, YAML loading, env precedence, TUI/bootstrap context controls including `max_context_age_minutes`, context-refresh token controls (`max_tokens`, derived refresh target), memory-recall classifier/window toggles, message-cadence config for self-reflection and auto-memory creation, and the first memory consolidation/recall-quality knobs (`memories_between_consolidation`, `l2_memory_relevance_distance_threshold`, `memory_cluster_similarity_threshold`, `max_memory_cluster_size`, `min_memory_cluster_size`, `reflect`) now exist; most config keys remain unimplemented |
| Tool schema compatibility | `partial` | `crates/elroy-tools/src/lib.rs`, `crates/elroy-app/src/lib.rs` | Canonical schema and provider adapters exist, and a growing set of executable memory/agenda tools now run through the shared live runtime; broader schema coverage and richer execution semantics remain unimplemented |
| Streaming status semantics | `partial` | `crates/elroy-llm/src/lib.rs`, `crates/elroy-core/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-tui/src/lib.rs`, `crates/elroy-cli/src/main.rs` | Event types exist, the core loop now supports lazy streamed tool loops plus Python-style tool-execution status events, normalized transcript messages are built alongside events, OpenAI/Anthropic live clients now parse SSE response streams, the shared runtime now exposes a Python-style global background-status registry, and the `ratatui` shell now renders streamed prompt updates, restart/greeting bootstrap streams, deferred context-refresh scheduling, deferred self-reflection scheduling for TUI user prompts, deferred auto-memory scheduling for streamed TUI prompts, Codex background-session status including the assistant-side completion-follow-up phase, cancel handling, polled background context-message tails, idle footer background-status text, and a snapshot refresh once background work completes so UI state can catch up to deferred mutations without overwriting the current foreground status line. The CLI TUI adapter now also dispatches deferred context refresh, auto-memory creation, and self-reflection through background workers so the footer can surface that shared background-status path rather than blocking inline, with direct regression coverage for both their running statuses and failure statuses, and local command-palette / slash-command form actions now run through the same background-worker footer model instead of blocking inline command execution. Broader background-status producers are still missing |
| TUI keyboard behavior | `partial` | `crates/elroy-tui/src/lib.rs` | Source of truth is `../elroy/UI.md`; focus toggling, pane switching, submit/open/archive/complete/delete actions, chat-mode prompt-history traversal with `Up`/`Down`, chat-mode `Tab` acceptance of runtime-provided plain-agenda completions plus slash-command names, zero-argument plus fully specified slash-command submit execution, basic slash-command modal form launch plus positional prefill for underspecified commands, command-form `Tab` acceptance of matching `name`-field suggestions before field navigation, launch-time command-form errors that stay attached to the open modal instead of closing it first, launch-time command-palette failures that now also keep the palette open instead of collapsing it before surfacing the error, unknown slash-prefixed chat input now also falls back to the ordinary chat-stream path instead of surfacing a local invalid-command error, and local command launch blocking while a chat stream is active, `Ctrl+P` command-palette opening plus typed filtering plus first-party system-command surfacing plus command selection into execute-or-form behavior, command-mode `j`/`k` and arrow movement now scrolling the conversation pane when it has focus instead of incorrectly mutating sidebar selection, and mouse-wheel scrolling now also moves conversation history from chat input or the conversation pane, while non-browse conversation output auto-follows the latest lines until the user intentionally scrolls history and later returns focus to input, post-response/post-cancel focus return to the chat input, live prompt-stream blocking/editability, `Ctrl+C` clear/cancel intents are now modeled and bound to terminal events, and canceled prompt streams now also have direct coverage that they do not trigger deferred self-reflection follow-up, while deferred context refresh now also waits for both foreground command actions and active prompt streams to finish instead of starting in the middle of a local foreground workflow. Background-completion snapshot reloads now also wait for both foreground command actions and active prompt streams to finish instead of refreshing sidebar/conversation snapshot state in the middle of a local foreground workflow, with direct loop-boundary regressions that the delayed refresh still lands once the blocking command or prompt path clears. The event-loop runtime tick now also has direct coverage that both a completed background `restart_session` command and a completed prompt stream with a pending restart request return `RestartRequested(...)` at the loop boundary instead of only exposing lower-level helper results, and the shared CLI/TUI restart-stream plus startup-resume adapters now also have direct coverage that their injected resume prompts are treated as non-persisted input instead of being written back as new user transcript lines. Background context polling now also waits for both foreground command workflows and active prompt streams to finish before appending newly arrived transcript lines. Sidebar section switches now reset the active selection to the first row instead of carrying a stale index over from the previous section, matching the Python UI’s first-item focus behavior when a different sidebar list becomes active. The chat composer now grows vertically for wrapped drafts instead of pinning all input to a fixed single-row box. The sidebar can now switch across Python-style memories/agenda/improvements/feature-requests/Codex sections, active feature requests can be opened and completed from the detail modal, a Codex-session sidebar section can be selected and opened read-only, the agenda sidebar now shows active tasks with Python-style due-time labels rather than plain-agenda-only rows, and opened sidebar items now use a detail-modal interaction with `Escape`/`Enter`/`q` close semantics and confirmed destructive actions. Rendered-message bookkeeping and background context-poll rendering now match the Python bootstrap/chat anchoring rules, including the mixed case where an older unseen interleaved context message exists but a newer trailing unseen suffix still needs to be rendered, and streamed prompt completion can now hand deferred auto-memory work off to the CLI-side background worker/footer path instead of forcing that post-turn work to finish inline before the UI regains control. Broader session workflows plus deeper command-form validation parity and fuller Textual-style command-palette system-command behavior are still missing |
| Persistence compatibility with existing user data | `partial` | `crates/elroy-db/src/lib.rs`, `crates/elroy-cli/src/main.rs` | Compatible file discovery plus rebuild of derived bootstrap, memory, agenda, and local context-message state exist; higher-level repository behavior is still missing |
| File-backed inspectable recallable content | `partial` | `crates/elroy-db/src/lib.rs`, `crates/elroy-app/src/lib.rs` | Recursive file inventory, frontmatter/body extraction, derived memory/agenda table rebuilds, and a first set of file-backed memory/agenda mutation helpers exist for markdown-backed data; active-memory bootstrap discovery still skips archived memory files so archive moves stay out of the active surface, and archived memory files now also sync back as inactive derived history rows, but repository-level sync behavior is not implemented yet |
| Memory recall quality | `partial` | `crates/elroy-app/src/lib.rs` | Prompt-time recall now exists via deterministic heuristic matching over active memories with recent transcript context, transcript-level dedupe, and Python-style role-prefixed recent context (`user:` / `assistant:`) in the recall query instead of the older Rust raw-text-only context window. Its fast non-reflect branch now also uses the Python-style synthetic `get_fast_recall` transcript shape with `recall_metadata`, instead of the older Rust-only `search_memories` wrapper payload, and it now surfaces relevant due items plus plain agenda items alongside memories instead of leaving mixed-item prompt recall to the reflective branch only. Persisted current-context fast-recall messages for pinned memories, due items, and plain tasks now also carry that same `recall_metadata` shape, so prompt-time recall dedupe can suppress items that are already pinned into transcript context instead of only suppressing earlier bootstrap recall payloads. That dedupe is now also ID-aware, so old recall metadata for an inactive same-name memory or agenda item does not incorrectly suppress a newer active replacement with the same logical name. It also has Python-style classifier enable/disable plus window controls, including explicit case-insensitive trivial greeting/acknowledgment skip coverage that now also tolerates surrounding punctuation and pure clarification-only prompts like `what?` / `huh`. Nontrivial prompts can now also take a first Python-closer model-backed classifier path that returns a structured recall decision before retrieval, with conservative fallback to recalling memories when classifier config or parsing fails. After heuristic retrieval, the prompt-time recall path now also has a first Python-style model-backed relevance filter over the selected recall candidates, and when heuristic overlap finds nothing it can now ask that same model-backed filter to inspect a broader bounded active candidate pool instead of only a tiny recent-candidate fallback, so semantically relevant older memories, due items, and agenda items can still surface instead of hard-failing on zero-overlap wording or being crowded out by recency alone. The prompt/runtime and recall-tool paths now also widen their upstream active-row fetch pools when a relevance model is available, so that broader semantic pass is not still starved by older Rust-side `50`/`20` prompt fetch caps or the older `limit * 3` tool preload caps. The model-backed semantic selectors themselves now also inspect a broader bounded candidate window instead of the older 25-candidate ceiling, so older relevant items that survive the widened fetch path are not still dropped before the relevance model ever sees them. That same best-effort relevance pass can now also prefer a semantically relevant memory, due item, or plain agenda item over a weaker lexical-overlap candidate instead of treating overlap hits as a hard gate once any overlap exists, and the full prompt runtime now has direct coverage of that semantic-priority behavior for memories, due items, and plain agenda items instead of only helper-level or memory-only runtime evidence. The shared runtime now also has direct `AppRuntime::process_message(...)` regressions that ordinary prompt-time fast recall is actually injected into the live prompt transcript and persisted back as the Python-style synthetic `get_fast_recall` tool payload, including a mixed memory/due-item/plain-agenda fast-recall payload on the real runtime path, and a two-turn runtime case proving that persisted fast-recall metadata suppresses duplicate reinjection on the next turn instead of only relying on helper-level dedupe coverage. The reflective branch now also uses that broader best-effort candidate expansion for due items and plain agenda items instead of staying overlap-only there while the fast branch improved, and the full prompt runtime now also has direct coverage that `reflect=true` can inject model-authored reflective recall content or suppress reflective recall entirely when the relevance model marks it irrelevant instead of relying only on helper-level reflective payload tests, plus a matching two-turn runtime case proving that persisted reflective recall metadata suppresses duplicate reflective reinjection on the next turn instead of reauthoring the same reflective payload repeatedly. The same style of best-effort relevance filtering now also applies to the user-facing `search_memories` and `examine_memories` selection path over memories, due items, and plain agenda items instead of leaving those tool surfaces purely overlap-based, and the live tool path now has direct runtime coverage that `search_memories` can prefer semantically relevant memory, due-item, and plain-agenda-item results over weaker lexical-overlap candidates while `examine_memories` can do the same for memory, due-item, and plain-agenda-item results instead of relying only on helper-level selector coverage. The runtime now caps injected recalled memories at the Python-style top 2 instead of overfilling the transcript with a third heuristic match, and the user-facing recall tools now also cap surfaced memories, due items, and plain agenda items to 2 per section even when a larger hidden Rust-side limit is passed, which keeps the Rust recall-tool output closer to Python’s shared top-2 recall store. The context-refresh summary prompt sent to the fast model is now also Python-closer, with a `Conversation Summary` header, timestamped user/assistant/tool lines, the Python-style explicit `Your word limit is 300. DO NOT EXCEED IT.` instruction, and a final `Messages from ... to ...` span instead of the thinner earlier Rust transcript formatting. Scenario-driven parity tests and embeddings/classifier behavior are still missing |
| Reminder and due-item surfacing | `partial` | `crates/elroy-app/src/lib.rs`, `crates/elroy-core/src/lib.rs` | Time-due items are now injected into model context through Python-style transient synthetic tool messages with explicit cleanup instructions, and the contextual-trigger surfacing path now exists with persisted current-context pinning and later-turn dedupe for matched reminders. That contextual path can now also use the same best-effort model-backed bounded candidate expansion as recall quality when wording overlap is zero, so semantically relevant reminders can still surface instead of being strictly overlap-only, and it can now prefer a semantically relevant reminder over a weaker lexical-overlap candidate instead of treating overlap hits as a hard gate. Its model-backed candidate pool is also now broader than the earlier tiny recent-only fallback, the runtime/tool preload path now feeds that broader pass from a wider active due-item source pool too, and the shared semantic candidate window is now wider than the older 25-candidate ceiling, so an older but still relevant contextual reminder can still be selected instead of being crowded out purely by recency, earlier low fetch caps, or the old small relevance-model window. It also now avoids re-injecting a contextual reminder when the same agenda item is already present in fast-recall metadata for the same turn, and prompt-runtime coverage now also proves the same suppression holds when the reminder is already pinned in persisted current context rather than only helper-level transcript fixtures. The shared runtime now also has a two-turn prompt workflow regression proving that once a contextual due item is surfaced and persisted on one turn, a later matching turn does not append a second copy of that contextual reminder pair. Hybrid timed-plus-context reminders no longer surface twice in one prompt turn by coming through both the timed-due injection path and prompt-time recall/context paths. The same recall-metadata dedupe is now ID-aware too, so a newly active same-name reminder can still surface after the older reminder row that previously used that name has already been recalled. The real shared runtime now also has end-to-end prompt workflow regressions that future timed reminders stay out of the live prompt, multiple simultaneous due reminders surface together, hybrid timed-plus-context reminders still surface when their time component is due, and a due timed reminder can surface, trigger a model-requested `delete_due_item`, and be removed from the active DB/file surfaces through the normal `AppRuntime::process_message(...)` path instead of only through lower-level orchestration tests. Richer reminder-selection behavior is still missing |
| Codex workflow support in v1 | `partial` | `crates/elroy-codex/src/lib.rs`, `crates/elroy-app/src/lib.rs`, `crates/elroy-cli/src/main.rs` | Persisted session metadata, async dispatch/resume lifecycle, isolated worktree handling, tool-surface inspection, recursive session-file discovery, background-status producer coverage for running sessions plus the assistant-side completion-follow-up phase, and background assistant follow-up behavior now exist. The real CLI/TUI adapter now also has direct regression coverage that background `dispatch_codex_session` and `resume_codex_session` commands drive those running and completion-follow-up statuses through the shared footer/polling boundary and then expose the completed Codex session back through snapshot/sidebar state. Broader UI workflow support is still missing |

## Intentional Deltas

Record accepted behavior changes here once they exist.

| Area | Delta | Rationale | Approved by | Date |
| --- | --- | --- | --- | --- |
| none yet | none | none | none | none |
